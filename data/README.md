# Data

Research data for the AI Risk Knowledge Graph project.

- `raw/`: original source files, kept unchanged
- `taxonomy/`: AI risk and AI system taxonomy source files
- `processed/`: notebook outputs and derived research artifacts
- `processed/outputs/`: exported JSON, CSV, RDF, or other generated outputs

Keep raw data immutable where possible. Put regenerated artifacts in `processed/`
so notebook experiments stay traceable.
