# One-command deploy:  docker compose up --build   ->  http://localhost:8000
services:
  workbench:
    build: .
    image: pair-ai
    container_name: pair-ai-workbench
    ports:
      - "8000:8000"
    restart: unless-stopped
